package com.klasnic.pos.model.services;

import com.klasnic.pos.model.catalogs.PPL;
import com.klasnic.pos.model.repositories.PPLRepository;

public interface PPLService	extends ActivableService<PPLRepository, PPL>{

	/*Usuario findByIdUsuario(String idUsuario);
	boolean login(String idUsuario, String password);*/
}
